using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class PlayerDataStorage
{
    PlayerDataStorage()
    {
        // load prefabs
        bulletPrefab = (GameObject)Resources.Load("Prefabs/bullet", typeof(GameObject));
    }

    // prefabs
    private GameObject bulletPrefab;
    private PlayerStateFlags _state;
    private int _bulletCount;
}*/

/*public sealed class PlayerDataStorage
{
    private static PlayerDataStorage instance = null;
    //private static readonly object padlock = new object();

    private PlayerDataStorage()
    {
        // load prefabs
        bulletPrefab = (GameObject)Resources.Load("Prefabs/bullet", typeof(GameObject));
    }

    public static PlayerDataStorage Instance
    {
        get
        {
            //lock (padlock)
            {
                if (instance == null)
                {
                    instance = new PlayerDataStorage();
                }
                return instance;
            }
        }
    }

    // enums

    // prefabs
    public GameObject bulletPrefab;

    // Player information

    

    // inner private variable, empty

    // public functions
    
}*/